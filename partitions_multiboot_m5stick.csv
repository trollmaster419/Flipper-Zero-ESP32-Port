# Name,   Type, SubType,   Offset,    Size,     Flags
# ---------------------------------------------------------------------------
# Multi-boot layout for 8 MB flash (M5StickC Plus2).
#
#   ota_0  = this ESP32 Flipper Zero port  (default boot target)
#   ota_1  = MEMZ Trojan payload            (the prank firmware)
#
# Switching is done via esp_ota_set_boot_partition() + reboot from the
# "Bruce" / "Other OS" menu entry. otadata starts erased (0xFF) so the
# bootloader boots ota_0 by default.
# ---------------------------------------------------------------------------
nvs,        data, nvs,       0x9000,    0x6000,
otadata,    data, ota,       0xf000,    0x2000,
phy_init,   data, phy,       0x11000,   0x1000,
ota_0,      app,  ota_0,     0x20000,   0x380000,
ota_1,      app,  ota_1,     0x3a0000,  0x80000,
littlefs,   data, spiffs,    0x420000,  0x3e0000,
