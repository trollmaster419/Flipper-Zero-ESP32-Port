build/
build_t_embed/
build_test/
build_s3/
build_esp32/
build_esp32_build.log
graphify-out/
.cache/
.idea/
__pycache__/
sdkconfig.old
.DS_Store
build_waveshare_c6/
.vscode/settings.json

# Local-dev tooling (not for upstream)
_flash_helper.py

# Multi-boot: the Bruce firmware checkout is managed by patchBruce.py
# (clone + git pull + tools/bruce_multiboot.patch), not checked in.
multi-boot/bruce/

# Managed components are pulled by IDF component manager from idf_component.yml
# + dependencies.lock; the source-of-truth is those two files.
managed_components/

# Sensitive: populated KeeLoq manufacturer-key table. Kept locally for testing,
# never published. Anchored path matches ONLY this file — keeloq_mfcodes_user and
# keeloq_mfcodes_user.example stay tracked (they're safe).
/applications/main/subghz/resources/subghz/assets/keeloq_mfcodes
